---
layout: layouts/base.njk
title: Blog
description: Insights on AI-powered biomarker intelligence, kidney care innovation, and the future of clinical monitoring from the Samaritan team.
permalink: /blog/
---

<section class="blog-hero">
  <div class="container">
    <p class="eyebrow eyebrow-left">Blog</p>
    <h1>Insights & Updates</h1>
    <p>Perspectives on AI in healthcare, biomarker science, and the future of clinical monitoring.</p>
  </div>
</section>

<section>
  <div class="container">
    <div class="blog-grid">
      {% set posts = collections.blog %}
      {% for post in posts %}
      <a href="{{ post.url }}" class="card card-white card-hoverable blog-card reveal{% if loop.index <= 3 %} reveal-delay-{{ loop.index0 }}{% endif %}">
        <div class="blog-card-body">
          <div class="blog-card-meta">
            <time datetime="{{ post.date | isoDate }}">{{ post.date | readableDate }}</time>
            &middot; {{ post.content | readingTime }}
          </div>
          <h3 class="blog-card-title">{{ post.data.title }}</h3>
          <p class="blog-card-excerpt">{{ post.data.description }}</p>
          {% if post.data.tags %}
          <div class="blog-card-tags">
            {% for tag in post.data.tags %}
              {% if tag != "post" %}
              <span class="tag">{{ tag }}</span>
              {% endif %}
            {% endfor %}
          </div>
          {% endif %}
        </div>
      </a>
      {% endfor %}
    </div>
  </div>
</section>
